output_dir: results/llm_smoke
conversation_file: conversations.jsonl
metrics_file: metrics.json

scenarios:
  - scenarios/retail_refund.yaml

agents:
  - scaffold_policy_aware
  - scaffold_naive

personas:
  - indirect_refusal
  - mitigated_request

variants:
  - en_direct
  - en_mitigated
  - mandarin_literal
  - mandarin_pragmatic

repeats: 1
turns: 3
